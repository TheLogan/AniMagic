import React from 'react';
import { RouteComponentProps } from 'react-router-dom';
import { Button } from '@material-ui/core';

interface IProps extends RouteComponentProps { }

export function EditClip(props: IProps) {
    return <div>
    </div>
}